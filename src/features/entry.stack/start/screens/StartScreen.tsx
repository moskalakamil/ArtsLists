import React from 'react';
import { View } from 'react-native';
import Start from '@/features/entry.stack/start/components/Start';

export const StartScreen = () => (
  <View className={'flex-1'}>
    <Start />
  </View>
);
